/**
 * @deprecated use prisma Role enum
 */
export enum Role {
  ADMIN = 'ADMIN',
  USER = 'USER',
}

/**
 * @deprecated use prisma Action enum
 */
export enum Action {
  RESERVE = 'RESERVE',
  CANCEL = 'CANCEL',
}
